// import type { RequestHandler } from '@builder.io/qwik-city';
// import { onRequest } from '~/routes/plugin@auth';

// export const onGet: RequestHandler = onRequest;
// export const onPost: RequestHandler = onRequest;
